import { GetCateUrlPipe } from './get-cate-url.pipe';

describe('GetCateUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCateUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
