import { NormalizeViPipe } from './normalize-vi.pipe';

describe('NormalizeViPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeViPipe();
    expect(pipe).toBeTruthy();
  });
});
